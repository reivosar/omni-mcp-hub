apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "omni-mcp-hub.fullname" . }}-config
  labels:
    {{- include "omni-mcp-hub.labels" . | nindent 4 }}
data:
  omni-config.yaml: |
    {{- .Values.config.omniConfig | nindent 4 }}