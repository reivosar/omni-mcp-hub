{
  "mcpServers": {
    "omni-docker": {
      "command": "docker",
      "args": ["exec", "-i", "omni-mcp-hub-local_sources", "node", "/app/dist/servers/server.js"],
      "env": {
        "MCP_MODE": "stdio"
      }
    }
  }
}