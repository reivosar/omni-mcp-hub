apiVersion: v1
kind: Service
metadata:
  name: {{ include "omni-mcp-hub.fullname" . }}
  labels:
    {{- include "omni-mcp-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omni-mcp-hub.selectorLabels" . | nindent 4 }}